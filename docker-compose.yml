services:
  pdf-fix:
    image: ghcr.io/aspendlove/pdffix:latest
    container_name: pdf-fix
    restart: always
    ports:
      - "8080:8080"
        environment:
          - GIN_MODE=release
        volumes:
          - ./config.yaml:/app/config.yaml
        # Optional: Resource limits to match what we discovered in dev
    
